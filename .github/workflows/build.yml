name: Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  release:
    types: [created]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black

    - name: Lint with flake8
      run: |
        flake8 jwl_backup_merger.py --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 jwl_backup_merger.py --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Check format with black
      run: |
        black --check jwl_backup_merger.py || true

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests with pytest
      run: |
        pytest tests/ -v --tb=short

    - name: Run syntax check
      run: |
        python -m py_compile jwl_backup_merger.py

    - name: Test --help
      run: |
        python jwl_backup_merger.py --help

  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build binary with PyInstaller
      run: |
        pyinstaller jwl_backup_merger.spec --clean

    - name: Verify binary exists
      run: |
        ls -la dist/
      shell: bash

    - name: Upload artifact (Linux)
      if: runner.os == 'Linux'
      uses: actions/upload-artifact@v4
      with:
        name: jwl-backup-merger-${{ runner.os }}-linux
        path: dist/jwl-backup-merger
        retention-days: 30

    - name: Upload artifact (Windows)
      if: runner.os == 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: jwl-backup-merger-${{ runner.os }}-windows
        path: dist/jwl-backup-merger.exe
        retention-days: 30

    - name: Upload artifact (macOS)
      if: runner.os == 'macOS'
      uses: actions/upload-artifact@v4
      with:
        name: jwl-backup-merger-${{ runner.os }}-macos
        path: dist/jwl-backup-merger
        retention-days: 30

  release:
    runs-on: ubuntu-latest
    if: github.event_name == 'release' && github.event.action == 'created'

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build Linux binary
      run: |
        pyinstaller jwl_backup_merger.spec --clean

    - name: Build Windows binary
      uses: JackMcKew/pyinstaller-action-windows@main
      with:
        spec: jwl_backup_merger.spec

    - name: Build macOS binary
      run: |
        pyinstaller jwl_backup_merger.spec --clean

    - name: Prepare release assets
      run: |
        mkdir -p release_assets
        cp dist/jwl-backup-merger release_assets/jwl-backup-merger-linux
        cp build/windows/dist/jwl-backup-merger/jwl-backup-merger.exe release_assets/jwl-backup-merger-windows.exe
        # macOS binary будет собран отдельно если нужно

    - name: Upload Release Assets
      uses: softprops/action-gh-release@v1
      with:
        files: |
          release_assets/jwl-backup-merger-linux
          release_assets/jwl-backup-merger-windows.exe
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
